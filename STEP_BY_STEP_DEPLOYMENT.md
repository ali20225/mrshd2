# 🚀 دليل النشر خطوة بخطوة - بالصور والتفاصيل

## 🎯 الهدف
نشر موقع "مرشدك المهني" على Cloudflare Pages للحصول على رابط دائم ومجاني.

---

## ✅ الموقع جاهز!

### 🔗 رابط تجريبي حالي (يعمل الآن):
```
https://8080-is9jx7b6mi1aaywoiecez-cbeee0f9.sandbox.novita.ai
```

⚠️ **هذا رابط مؤقت للاختبار فقط**. لن يعمل بعد إغلاق الجلسة.

### 📦 مستودع GitHub (دائم):
```
https://github.com/ali20225/mrshd2
```

---

## 🎬 خطوات النشر على Cloudflare Pages

### المرحلة 1️⃣: إنشاء حساب Cloudflare

#### الخطوة 1: التسجيل
1. افتح متصفحك واذهب إلى: **https://dash.cloudflare.com/sign-up**
2. أدخل بريدك الإلكتروني وكلمة المرور
3. اضغط **Sign Up**
4. تحقق من بريدك الإلكتروني وفعّل الحساب

#### الخطوة 2: تسجيل الدخول
1. اذهب إلى: **https://dash.cloudflare.com/login**
2. سجّل الدخول بحسابك

---

### المرحلة 2️⃣: إنشاء مشروع Pages

#### الخطوة 1: الانتقال إلى Pages
1. من لوحة التحكم الرئيسية
2. في القائمة الجانبية اليسرى، اضغط على **Workers & Pages**
3. ستجد نفسك في صفحة المشاريع

#### الخطوة 2: إنشاء مشروع جديد
1. اضغط على زر **Create application** (أزرق، أعلى اليمين)
2. ستظهر نافذة بخيارين:
   - **Workers** (تخطاه)
   - **Pages** ← **اختر هذا**
3. اضغط على **Pages**

#### الخطوة 3: ربط GitHub
1. بعد اختيار Pages، ستظهر لك خيارات:
   - **Connect to Git** ← **اختر هذا**
   - Upload assets (تخطاه)
2. اضغط على **Connect to Git**

#### الخطوة 4: اختيار GitHub
1. ستظهر لك خيارات:
   - **GitHub** ← **اختر هذا**
   - GitLab (تخطاه)
2. اضغط على **GitHub**

#### الخطوة 5: منح الصلاحيات
1. ستُفتح نافذة GitHub للمصادقة
2. اضغط **Authorize Cloudflare Pages**
3. أدخل كلمة مرور GitHub إذا طُلب منك
4. اختر:
   - **All repositories** (كل المستودعات)
   - أو **Only select repositories** ثم اختر **ali20225/mrshd2**
5. اضغط **Install & Authorize**

#### الخطوة 6: اختيار المستودع
1. ستعود إلى صفحة Cloudflare
2. في قائمة المستودعات، ابحث عن: **ali20225/mrshd2**
3. اضغط على اسم المستودع

#### الخطوة 7: إعداد المشروع
ستظهر صفحة "Set up builds and deployments"

**املأ الحقول كالتالي:**

```
Project name:             murshedak-almahani
                          (أو أي اسم تريده بالإنجليزية بدون مسافات)

Production branch:        main

Build command:            npm run build

Build output directory:   dist

Root directory:           (اتركه فارغاً)
```

**Environment variables**: (اتركه فارغاً الآن)

8. اضغط **Save and Deploy** في الأسفل

---

### المرحلة 3️⃣: انتظار البناء والنشر

#### الخطوة 1: مراقبة عملية البناء
1. ستُنقل إلى صفحة البناء تلقائياً
2. سترى سجل البناء (Build log) يتحرك
3. انتظر 2-5 دقائق

#### الخطوة 2: نجاح النشر
1. عندما تنتهي العملية، سترى:
   - ✅ **Build successful**
   - 🚀 **Deployment successful**
2. سيظهر لك الرابط في الأعلى:
   ```
   https://murshedak-almahani.pages.dev
   ```

⚠️ **لكن الموقع لن يعمل بشكل كامل بعد!** نحتاج لإعداد قاعدة البيانات.

---

### المرحلة 4️⃣: إنشاء قاعدة البيانات D1

#### الخطوة 1: الانتقال إلى D1
1. من القائمة الجانبية، اضغط على **Storage & Databases**
2. ستظهر لك قائمة منسدلة، اختر **D1**

#### الخطوة 2: إنشاء قاعدة بيانات
1. اضغط على **Create database** (زر أزرق)
2. في صفحة الإنشاء:
   ```
   Database name: webapp-production
   ```
3. اضغط **Create**

#### الخطوة 3: حفظ Database ID
1. بعد الإنشاء، ستُفتح صفحة قاعدة البيانات
2. ستجد **Database ID** في الأعلى
3. مثال: `xxxx-xxxx-xxxx-xxxx-xxxx`
4. **انسخه واحفظه** (سنحتاجه لاحقاً)

---

### المرحلة 5️⃣: ربط قاعدة البيانات بالمشروع

#### الخطوة 1: العودة إلى المشروع
1. من القائمة الجانبية، اضغط **Workers & Pages**
2. اضغط على اسم مشروعك: **murshedak-almahani**

#### الخطوة 2: إعدادات Functions
1. من التبويبات في الأعلى، اضغط **Settings**
2. من القائمة الجانبية، اضغط **Functions**
3. انزل للأسفل حتى تجد: **D1 database bindings**

#### الخطوة 3: إضافة Binding
1. اضغط على **Add binding** أو **Edit bindings**
2. املأ الحقول:
   ```
   Variable name:    DB
   D1 database:      (اختر من القائمة) webapp-production
   ```
3. اضغط **Save**

---

### المرحلة 6️⃣: تطبيق Migrations على قاعدة البيانات

⚠️ **هذه الخطوة مهمة جداً!** بدونها لن تعمل قاعدة البيانات.

#### طريقة 1: عبر Wrangler CLI (موصى به)

**على جهازك (إذا كان لديك Node.js):**

```bash
# 1. تسجيل الدخول
npx wrangler login

# سيفتح متصفح للمصادقة، اضغط Allow

# 2. تطبيق migrations
npx wrangler d1 migrations apply webapp-production

# ستُسأل: "Ok to apply 1 migration(s)? yes"
# اكتب: yes
```

#### طريقة 2: عبر واجهة Cloudflare

1. ارجع إلى **Storage & Databases** → **D1**
2. اضغط على قاعدة البيانات: **webapp-production**
3. اضغط على تبويب **Console**
4. انسخ والصق هذا الكود SQL بالكامل:

```sql
-- Create users table
CREATE TABLE IF NOT EXISTS users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  class TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Create holland_answers table
CREATE TABLE IF NOT EXISTS holland_answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  question_id INTEGER NOT NULL,
  answer INTEGER NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Create mi_answers table
CREATE TABLE IF NOT EXISTS mi_answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  question_id INTEGER NOT NULL,
  answer INTEGER NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Create reports table
CREATE TABLE IF NOT EXISTS reports (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  holland_scores TEXT NOT NULL,
  mi_scores TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Create admin_stats view
CREATE VIEW IF NOT EXISTS admin_stats AS
SELECT 
  (SELECT COUNT(*) FROM users) as total_users,
  (SELECT COUNT(*) FROM reports) as total_reports;
```

5. اضغط **Execute**
6. يجب أن ترى: ✅ **Success**

---

### المرحلة 7️⃣: إعادة النشر (Redeploy)

#### لماذا؟
بعد ربط قاعدة البيانات، نحتاج لإعادة النشر لتطبيق التغييرات.

#### كيف؟
1. ارجع إلى مشروعك: **Workers & Pages** → **murshedak-almahani**
2. اضغط على تبويب **Deployments**
3. في أول deployment (الأحدث)، ستجد 3 نقاط `⋮` على اليمين
4. اضغط عليها واختر **Retry deployment**
5. انتظر 2 دقيقة

---

## 🎉 موقعك جاهز الآن!

### 🌐 الرابط النهائي الدائم:
```
https://murshedak-almahani.pages.dev
```

أو حسب الاسم الذي اخترته:
```
https://[اسم-مشروعك].pages.dev
```

---

## ✅ اختبار الموقع

### 1. افتح الرابط في المتصفح
### 2. يجب أن ترى:
- ✅ الصفحة الرئيسية للموقع
- ✅ نموذج إدخال الاسم والصف
- ✅ أسئلة مقياس هولاند
- ✅ أسئلة الذكاءات المتعددة

### 3. جرّب الموقع:
- أدخل اسماً وصفاً
- أجب على الأسئلة
- احصل على التقرير

---

## 🔄 التحديثات المستقبلية

الموقع متصل بـ GitHub. أي تحديث تقوم به:

```bash
git add .
git commit -m "وصف التحديث"
git push
```

سيُنشر تلقائياً على Cloudflare Pages خلال دقائق!

---

## 🎓 مشاركة الموقع مع الطلاب

### الرابط:
```
https://murshedak-almahani.pages.dev
```

### طريقة الاستخدام:
1. افتح الرابط
2. أدخل الاسم والصف
3. أجب على 54 سؤال
4. احصل على تقريرك الشامل
5. يمكن طباعته أو تصديره

---

## 🆘 حل المشاكل الشائعة

### مشكلة: "This site can't be reached"
**الحل:**
- تأكد من أنك أكملت خطوة ربط قاعدة البيانات
- جرّب Redeploy من Deployments

### مشكلة: "Database error"
**الحل:**
- تأكد من تطبيق Migrations على قاعدة البيانات
- راجع المرحلة 6️⃣

### مشكلة: "Build failed"
**الحل:**
- تأكد من أن إعدادات البناء صحيحة:
  - Build command: `npm run build`
  - Build output: `dist`

---

## 💡 نصائح إضافية

1. ✅ **دومين مخصص**: يمكنك إضافة دومينك الخاص من Settings → Custom domains
2. ✅ **SSL/HTTPS**: تلقائي ومجاني من Cloudflare
3. ✅ **Analytics**: يمكنك تفعيل Web Analytics من Settings
4. ✅ **حماية**: يمكنك إضافة Cloudflare Access للحماية

---

## 📞 الدعم

- **الوثائق**: راجع ملفات `.md` في المستودع
- **Cloudflare Docs**: https://developers.cloudflare.com/pages
- **GitHub**: https://github.com/ali20225/mrshd2

---

<div align="center">

# 🌟 استمتع بموقعك!

**صُنع بـ ❤️ للإرشاد المهني**

</div>
